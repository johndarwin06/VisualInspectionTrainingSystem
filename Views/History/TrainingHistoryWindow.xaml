<Window
    x:Class="VisualInspectionTrainingSystem.Views.History.TrainingHistoryWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    Title="My Training History"
    Width="1120"
    Height="720"
    MinWidth="760"
    MinHeight="540"
    AutomationProperties.Name="My training history"
    Background="{StaticResource App.LightMutedSurfaceBrush}"
    KeyboardNavigation.TabNavigation="Cycle"
    ResizeMode="CanResize"
    WindowStartupLocation="CenterOwner">
    <Grid Margin="28" Style="{StaticResource ContentEntranceGridStyle}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="18" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="16" />
            <RowDefinition Height="*" />
            <RowDefinition Height="14" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock AutomationProperties.HeadingLevel="Level1" FontSize="28" FontWeight="Bold" Foreground="{StaticResource App.LightTextBrush}" Text="My Training History" />
                <TextBlock Margin="0,5,0,0" Foreground="{StaticResource App.LightSecondaryTextBrush}" Text="Review your completed training sessions and administrator-reviewed results." TextWrapping="Wrap" />
            </StackPanel>
            <Button Grid.Column="1" MinWidth="120" AutomationProperties.Name="Refresh training history" Command="{Binding RefreshCommand}" Style="{StaticResource PrimaryActionButtonStyle}">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="Refresh" Style="{StaticResource AppIconStyle}" />
                    <TextBlock Text="Refresh" />
                </StackPanel>
            </Button>
        </Grid>
        <Border Grid.Row="2" Padding="18" Background="{StaticResource App.LightSurfaceBrush}" BorderBrush="{StaticResource App.LightBorderBrush}" BorderThickness="1" CornerRadius="10" Style="{StaticResource InteractiveCardStyle}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*" MinWidth="180" />
                    <ColumnDefinition Width="12" />
                    <ColumnDefinition Width="*" MinWidth="130" />
                    <ColumnDefinition Width="12" />
                    <ColumnDefinition Width="*" MinWidth="130" />
                    <ColumnDefinition Width="12" />
                    <ColumnDefinition Width="*" MinWidth="165" />
                    <ColumnDefinition Width="12" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="6" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <TextBlock Grid.Column="0" Style="{StaticResource FieldLabelStyle}" Text="Search" />
                <TextBlock Grid.Column="2" Style="{StaticResource FieldLabelStyle}" Text="Completed from" />
                <TextBlock Grid.Column="4" Style="{StaticResource FieldLabelStyle}" Text="Completed through" />
                <TextBlock Grid.Column="6" Style="{StaticResource FieldLabelStyle}" Text="Review status" />
                <TextBox Grid.Row="2" Grid.Column="0" AutomationProperties.Name="Search by session or image filename" Style="{StaticResource AccessibleTextBoxStyle}" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" ToolTip="Search by exact session number or image filename" />
                <DatePicker Grid.Row="2" Grid.Column="2" AutomationProperties.Name="Completed from date" SelectedDate="{Binding StartDate}" Style="{StaticResource AccessibleDatePickerStyle}" />
                <DatePicker Grid.Row="2" Grid.Column="4" AutomationProperties.Name="Completed through date" SelectedDate="{Binding EndDate}" Style="{StaticResource AccessibleDatePickerStyle}" />
                <ComboBox Grid.Row="2" Grid.Column="6" MinHeight="40" Padding="10,5" AutomationProperties.Name="Review status filter" ItemsSource="{Binding ReviewStatusOptions}" SelectedItem="{Binding SelectedReviewStatus}" />
                <Button Grid.Row="2" Grid.Column="8" MinWidth="100" AutomationProperties.Name="Clear history filters" Command="{Binding ClearFiltersCommand}" Style="{StaticResource SecondaryActionButtonStyle}">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="FilterOffOutline" Style="{StaticResource AppIconStyle}" />
                        <TextBlock Text="Clear" />
                    </StackPanel>
                </Button>
            </Grid>
        </Border>
        <Border Grid.Row="4" Background="{StaticResource App.LightSurfaceBrush}" BorderBrush="{StaticResource App.LightBorderBrush}" BorderThickness="1" CornerRadius="10">
            <Grid>
                <DataGrid x:Name="HistoryGrid" Margin="1" AutomationProperties.Name="Completed training sessions" AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False" EnableColumnVirtualization="True" EnableRowVirtualization="True" HeadersVisibility="Column" IsReadOnly="True" ItemsSource="{Binding Sessions}" RowHeaderWidth="0" SelectionMode="Single" VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling">
                    <DataGrid.Columns>
                        <DataGridTextColumn Width="145" Binding="{Binding CompletionTimeText}" Header="Completed" />
                        <DataGridTextColumn Width="90" Binding="{Binding DurationText}" Header="Duration" />
                        <DataGridTextColumn Width="85" Binding="{Binding TotalQuestions}" Header="Questions" />
                        <DataGridTextColumn Width="70" Binding="{Binding UserGoodAnswers}" Header="GOOD" />
                        <DataGridTextColumn Width="60" Binding="{Binding UserNgAnswers}" Header="NG" />
                        <DataGridTextColumn Width="90" Binding="{Binding ReviewedAnswers}" Header="Reviewed" />
                        <DataGridTextColumn Width="80" Binding="{Binding PendingAnswers}" Header="Pending" />
                        <DataGridTextColumn Width="105" Binding="{Binding ReviewedAccuracyText}" Header="Accuracy*" />
                        <DataGridTemplateColumn Width="150" Header="Review Status">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border Margin="4" Padding="8,4" HorizontalAlignment="Left" CornerRadius="10">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="{StaticResource App.WarningSurfaceBrush}" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding ReviewStatusText}" Value="Reviewed"><Setter Property="Background" Value="{StaticResource App.SuccessSurfaceBrush}" /></DataTrigger>
                                                    <DataTrigger Binding="{Binding ReviewStatusText}" Value="Partially Reviewed"><Setter Property="Background" Value="{StaticResource App.InfoSurfaceBrush}" /></DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock FontWeight="SemiBold" Text="{Binding ReviewStatusText}">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="{StaticResource App.WarningBrush}" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding ReviewStatusText}" Value="Reviewed"><Setter Property="Foreground" Value="{StaticResource App.SuccessBrush}" /></DataTrigger>
                                                        <DataTrigger Binding="{Binding ReviewStatusText}" Value="Partially Reviewed"><Setter Property="Foreground" Value="{StaticResource App.InfoBrush}" /></DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Width="110" Header="Result">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button MinWidth="88" Margin="4" AutomationProperties.Name="Open training result" Command="{Binding DataContext.OpenResultCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}" Content="View Result" Style="{StaticResource SecondaryActionButtonStyle}" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="{Binding IsEmpty, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <materialDesign:PackIcon Width="44" Height="44" HorizontalAlignment="Center" Foreground="{StaticResource App.LightSecondaryTextBrush}" Kind="History" />
                    <TextBlock Margin="0,12,0,0" FontSize="16" Foreground="{StaticResource App.LightSecondaryTextBrush}" Text="{Binding EmptyStateText}" TextAlignment="Center" TextWrapping="Wrap" />
                </StackPanel>
                <Border Background="{StaticResource App.BusyScrimBrush}" Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <ProgressBar Width="220" Height="6" IsIndeterminate="True" />
                        <TextBlock Margin="0,14,0,0" FontSize="16" Foreground="White" Text="Loading your training history..." />
                    </StackPanel>
                </Border>
            </Grid>
        </Border>
        <Grid Grid.Row="6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Foreground="{StaticResource App.LightSecondaryTextBrush}" Style="{StaticResource StatusMessageTextStyle}" Text="{Binding StatusMessage}" TextWrapping="Wrap" />
                <TextBlock Margin="0,3,0,0" FontSize="11" Foreground="{StaticResource App.LightSecondaryTextBrush}" Text="* Accuracy uses reviewed answers only; pending answers are excluded." />
            </StackPanel>
            <Button Grid.Column="1" MinWidth="130" AutomationProperties.Name="Load more training sessions" Command="{Binding LoadMoreCommand}" Content="Load More" Style="{StaticResource SecondaryActionButtonStyle}" />
        </Grid>
    </Grid>
</Window>
